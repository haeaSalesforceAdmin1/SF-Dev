/**
* @date 09/28/2020
* @description trigger for HAEAUserProvisioning__c
*/
trigger HMAUserProvisioningTriggerTemp on Contact (before insert, before update, before delete, after insert, after update, after delete, after undelete) {
   /*bak Trigger_Framework.createHandler(HAEAUserProvisioning__c.sObjectType); */
}