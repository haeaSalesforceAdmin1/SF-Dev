<aura:component>

    <lightning:overlayLibrary aura:id="overlayLib"/>

    <lightning:button name="cancel" label="Cancel" onclick="{!c.handleCancel}"/>
    <lightning:button name="confirm" label="Confirm" onclick="{!c.handleConfirm}"/>

</aura:component>